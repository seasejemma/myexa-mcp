# Must include the /v1/exa prefix.
KEYPOOL_EXA_BASE_URL=https://keypool.example/v1/exa

# Client-side only. Never configure this on Deno Deploy.
KEYPOOL_TOKEN=replace-me

# Local operator only. Never configure this in GitHub Actions or Deno runtime.
DENO_DEPLOY_TOKEN=replace-me

PORT=8000
